# Копируй в .env.local для локальной разработки. Оставь пустым — тот же origin + Vite proxy /api.
# Если фронт на 5173, а API на 8080 без proxy:
# VITE_APP_ORIGIN=http://localhost:8080
VITE_APP_ORIGIN=
