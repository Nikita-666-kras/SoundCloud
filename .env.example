# Скопируй в .env в корне репозитория на сервере (не коммитить).
# docker compose подставит переменные автоматически.

# Пароль PostgreSQL (смени в проде)
POSTGRES_PASSWORD=сгенерируй_надёжный_пароль

# JWT (openssl rand -base64 48)
JWT_SECRET=сгенерируй_длинную_случайную_строку
